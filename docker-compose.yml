version: "3.8"

networks:
  ewb-net:
    driver: bridge

services:
  restapi:
    build: ./restapi
    container_name: ewb-restapi
    ports:
      - 80:80
    environment:
      SOLR_URL: http://solr:8983
    depends_on:
      - solr
    networks:
      - ewb-net
    volumes:
      - ./data/source:/data/source
      - ./data/results:/data/results

  solr:
    image: solr:9.1.1
    container_name: ewb-solr
    restart: always
    volumes:
      - ./db/data/solr:/var/solr
      #- ./logs/solr:/opt/solr/server/logs
      - ./solr-ewb-jensen-shanon-distance-plugin/solr-js-plugin/target:/opt/solr/dist/plugins
      - ./solr_config:/opt/solr/server/solr
    ports:
      - 8983:8983
    entrypoint:
      - docker-entrypoint.sh
      - solr
      - start
      - -f
      - -c
      - -z
      - zoo:2181
      - -a
      - "-Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=n,address=1044 -Djute.maxbuffer=0x5000000"
    networks:
      - ewb-net

  zoo:
    image: zookeeper
    container_name: ewb-zoo
    restart: always
    ports:
      - 2180:8080
      - 2181:2181
    environment:
      - JVMFLAGS=-Djute.maxbuffer=50000000
    volumes:
      - ./db/data/zoo/data:/data
      - ./db/data/zoo/logs:/datalog
    networks:
      - ewb-net

  solr-config:
    build: ./solr_config
    container_name: ewb-solr_config
    ports:
      - 81:81
    depends_on:
      - solr
      - zoo
    networks:
      - ewb-net
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./solr_config/bash_scripts:/bash_scripts
    command: 
      - sh
      - -c
      - 'chmod +x /bash_scripts/init_config.sh && bash_scripts/init_config.sh'